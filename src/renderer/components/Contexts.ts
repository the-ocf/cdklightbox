import { createContext } from 'react';

export const StageRefContext = createContext({} as any);
export const ScaleContext = createContext({} as any);
export const LevelFilterContext = createContext({} as any);
